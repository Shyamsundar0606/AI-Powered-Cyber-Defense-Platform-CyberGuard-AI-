import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = {
  title: "CyberGuard AI | Detection Engineering",
};

export default function DetectionEngineeringLayout({ children }: { children: ReactNode }) {
  return children;
}

